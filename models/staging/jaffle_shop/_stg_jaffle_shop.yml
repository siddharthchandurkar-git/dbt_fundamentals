models:
  - name: stg_jaffle_shop__customers
    description: The grain of this table is one unique cutomer per row
    columns:
      - name: customer_id
        description: Primary key for the customers Table
        data_tests:
          - unique
          - not_null
          
  - name: stg_jaffle_shop__orders
    description: One order per row
    columns:
      - name: order_id
        description: Primary key for the orders table
        data_tests:
          - unique
          - not_null
      - name: order_status  # Changed from status to match your SQL alias!
        description: "{{ doc('order_status')}}"
        data_tests:
          - accepted_values:
              arguments:
                values:
                  - completed
                  - shipped
                  - placed
                  - return_pending
                  - returned
      - name: customer_id
        data_tests:
          - relationships:
              arguments:
                field: customer_id
                to: ref('stg_jaffle_shop__customers')